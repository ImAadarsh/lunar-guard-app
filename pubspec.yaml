name: lunar_security_guard
description: Lunar Security — field guard operations (UI shell; API integration TBD).
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1
  dio: ^5.7.0
  flutter_secure_storage: ^9.2.2
  provider: ^6.1.2
  geolocator: ^12.0.0
  image_picker: ^1.1.2
  mobile_scanner: ^6.0.2
  google_maps_flutter: ^2.9.0
  local_auth: ^2.3.0
  sqflite: ^2.4.2+1
  path: ^1.9.1
  connectivity_plus: ^7.1.1
  file_picker: ^11.0.2
  flutter_local_notifications: ^21.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
